여기에 적어서 공유해요!!!
일단 제가썻던거 올릴테니까...sql 문 추가해고 안되는것같던데 
그러니까 일단 자기디비에 만든다음에 확실히정한후에해요 .. 

CREATE DATABASE 데이터베이스이름 DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;

GRANT ALL ON 데이터베이스이름.* TO '아이디' IDENTIFIED BY '비번';

use 권한 받은 데이터베이스이름;


CREATE TABLE users (
	id INT AUTO_INCREMENT PRIMARY KEY, 
	userid VARCHAR(100) NOT NULL UNIQUE,
	name VARCHAR(100),
	pwd VARCHAR(255) NOT NULL, 
	email VARCHAR(100) UNIQUE,
	country VARCHAR(100),
	gender CHAR(1) NOT NULL,
	favorites VARCHAR(255)
);
여기적어서 공유해요~!! 두개던 세게던 형식만 정해주시면 각자만들면되니까...
